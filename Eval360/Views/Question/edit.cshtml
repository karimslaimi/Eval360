@model Question

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}




<div class="container">

    <div class="card">
        <div class="card-body">
            <div class="card-title">
                <h1>Ajouter une question</h1>
            </div>



            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true, "", new { @class = "text-theme-6" })
                <div class="row">
                    <div class="mb-3 col-md-6">
                        @Html.LabelFor(m => m.libelle, new { @class = "form-label" })
                        @Html.TextBoxFor(m => m.libelle, new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.libelle, "", new { @class = "text-danger" })
                    </div> 
                    <div class="mb-3 col-md-6">
                        @Html.LabelFor(m => m.axeEval, new { @class = "form-label" })
                        @Html.DropDownListFor(m => m.axeEval.id,(SelectList) ViewBag.axeList, "Selectionner un Axe", new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.axeEval, "", new { @class = "text-danger" })
                    </div>
                    <div class="mb-3 col-md-6">
                        @Html.LabelFor(m => m.isEnabled, "Activé", new { @class = "form-label" })
                        @Html.CheckBoxFor(m => m.isEnabled, new { @class = "form-check-input" })
                        @Html.ValidationMessageFor(m => m.isEnabled, "", new { @class = "text-danger" })
                    </div>

                    <button type="submit" class="btn btn-primary">Enregistrer</button>
                </div>
            }
        </div>
    </div>
</div>